import React from 'react';
import LogoImg from '../../assets/logo.png';

const Header = () => {
    return (
        <div>
            <img src = {LogoImg}/>
        </div>
    );
};

export default Header;